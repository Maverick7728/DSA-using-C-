#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    int num, digit, ans = 0;
    cin >> num >> digit;
    while (num != 0)
    {
        if (num % 10 == digit)
        {
            ans = ans + 1;
        }
        num = num / 10;
    }
    cout << ans;
    return 0;
}
